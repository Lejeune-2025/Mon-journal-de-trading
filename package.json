{
  "name": "journal-trading",
  "private": true,
  "type": "module",
  "description": "Journal de trading professionnel — local + sync cloud optionnelle (Vercel Postgres)",
  "scripts": {
    "start": "npx --yes serve . -p 3000",
    "db:schema": "node scripts/print-schema.js"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  }
}
